[package]
name = "lowering"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
swc_ecma_ast = "15.0.0"
ir = { path = "../ir" }

[lib]
name = "lowering"
path = "src/lib.rs"
