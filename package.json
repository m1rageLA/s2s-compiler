{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:compiler && tsc -p tsconfig.json",
    "build:compiler": "node compiler/scripts/build-native.js && tsc -p compiler/tsconfig.json",
    "start": "node index.js",
    "dev": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3"
  }
}
