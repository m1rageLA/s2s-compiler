[package]
name = "lowering"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
swc_ecma_ast = "16.0.0"
ir = { path = "../ir_types" }

[dev-dependencies]
swc_common = "15.0.0"
swc_atoms = "7.0.0"

[lib]
name = "lowering"
path = "src/lib.rs"
