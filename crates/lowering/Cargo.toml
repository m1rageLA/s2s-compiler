[package]
name = "lowering"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
swc_ecma_ast = "15.0.0"
ir = { path = "../ir" }

[dev-dependencies]
swc_common = "14.0.3"

[lib]
name = "lowering"
path = "src/lib.rs"
